services:
  image-processor:
    build: .
    container_name: vinvinvin-image-processor
    restart: unless-stopped

    environment:
      - TZ=Asia/Seoul

    volumes:
      - ./config:/app/config
      - ./your/local/images:/app/images
